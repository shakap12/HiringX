server:
  port: 8082

spring:
  application:
    name: JOB-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/hiringx
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver



  jpa:
    hibernate:
      ddlAuto: update
    showSql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
